name: Build-tinycore

on:
  push:
    branches: [ main, master ]
    paths:
      - 'build-tinycore.sh'
      - '.github/workflows/Build-tinycore.yml'
  workflow_dispatch:
    inputs:
      openwrt_image_url:
        description: 'OpenWRT镜像URL（可选）'
        required: false

jobs:
  build:
    runs-on: ubuntu-latest  # GitHub Actions已经预装了Docker
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: 设置Docker环境
      run: |
        echo "🐳 Docker环境检查..."
        docker --version
        docker info | grep -E "(Server Version|Containers|Images)"
        echo "✅ Docker环境正常"
    
    - name: 准备构建文件
      run: |
        # 创建修复后的Dockerfile
        cat > Dockerfile.openwrt-builder << 'DOCKEREOF'
        FROM ubuntu:22.04
        
        # 设置非交互模式
        ENV DEBIAN_FRONTEND=noninteractive
        
        # 安装构建工具（使用正确的包名）
        RUN apt-get update && apt-get install -y \
            genisoimage \
            syslinux \
            isolinux \
            squashfs-tools \
            pv \
            xorriso \
            mtools \
            dosfstools \
            # GRUB引导工具
            grub-pc-bin \
            grub-efi-amd64-bin \
            grub-efi-ia32-bin \
            grub2-common \
            wget \
            curl \
            xz-utils \
            zstd \
            lz4 \
            upx-ucl \
            cpio \
            gzip \
            file \
            findutils \
            coreutils \
            jq \
            # 网络工具
            iproute2 \
            net-tools \
            && rm -rf /var/lib/apt/lists/*
        
        # 检查并安装mkisofs（如果genisoimage未提供）
        RUN if ! command -v mkisofs >/dev/null 2>&1 && ! command -v genisoimage >/dev/null 2>&1; then \
            apt-get update && apt-get install -y mkisofs; \
        fi
        
        # 创建工作目录
        WORKDIR /builder
        
        # 创建必要的目录
        RUN mkdir -p /mnt /output
        
        # 设置构建脚本（将在运行时挂载）
        ENTRYPOINT ["bash", "-c"]
        CMD ["echo '请挂载构建脚本并运行'"]
        DOCKEREOF
        
        # 创建最小化构建脚本（防止挂载失败时使用）
        cat > build-tinycore-minimal.sh << 'SCRIPTEOF'
        #!/bin/bash
        echo "最小化构建脚本"
        echo "请确保正确挂载了完整的构建脚本"
        exit 1
        SCRIPTEOF
        chmod +x build-tinycore-minimal.sh
    
    - name: 准备OpenWRT镜像
      id: prepare_image
      run: |
        echo "📥 准备OpenWRT镜像..."
        
        # 创建目录
        mkdir -p assets
        
        # 检查是否有提供的URL
        if [[ -n "${{ github.event.inputs.openwrt_image_url }}" ]]; then
            echo "使用提供的OpenWRT URL"
            OPENWRT_URL="${{ github.event.inputs.openwrt_image_url }}"
        else
            # 使用默认镜像
            OPENWRT_URL="https://github.com/sirpdboy/openwrt/releases/download/v2024.09.01/2024.09.01-x86-64-generic-squashfs-combined.img.gz"
        fi
        
        echo "下载URL: $OPENWRT_URL"
        
        # 下载镜像
        if wget -q --tries=3 --timeout=30 -O "assets/ezopwrt.img.gz" "$OPENWRT_URL"; then
            echo "✅ 下载成功"
            
            # 解压
            if gzip -t "assets/ezopwrt.img.gz" 2>/dev/null; then
                echo "解压gzip文件..."
                gzip -d "assets/ezopwrt.img.gz"
                echo "✅ 解压完成"
            else
                echo "不是gzip文件，直接使用"
                mv "assets/ezopwrt.img.gz" "assets/ezopwrt.img"
            fi
            
            # 验证文件
            if [ -f "assets/ezopwrt.img" ]; then
                IMG_SIZE=$(du -h "assets/ezopwrt.img" | cut -f1)
                echo "镜像大小: $IMG_SIZE"
                echo "img_path=assets/ezopwrt.img" >> $GITHUB_OUTPUT
            else
                echo "❌ 镜像文件创建失败"
                exit 1
            fi
        else
            echo "❌ 下载失败，创建测试镜像"
            dd if=/dev/zero of=assets/ezopwrt.img bs=1M count=10
            echo "img_path=assets/ezopwrt.img" >> $GITHUB_OUTPUT
        fi
    
    - name: 构建Docker镜像
      run: |
        echo "🐳 构建Docker镜像..."
        
        # 构建镜像
        docker build -t openwrt-builder -f Dockerfile.openwrt-builder .
        
        # 验证镜像
        echo "镜像列表:"
        docker images | grep openwrt-builder
        
        echo "✅ Docker镜像构建完成"
    
    - name: 运行ISO构建
      run: |
        echo "🔨 开始构建ISO..."
        
        # 检查必要文件
        if [ ! -f "assets/ezopwrt.img" ]; then
            echo "❌ 找不到OpenWRT镜像"
            exit 1
        fi
        
        if [ ! -f "build-tinycore.sh" ]; then
            echo "❌ 找不到构建脚本"
            exit 1
        fi
        
        chmod +x build-tinycore.sh
        
        echo "构建参数:"
        echo "  输入镜像: assets/ezopwrt.img"
        echo "  输出目录: output/"
        echo "  脚本: build-tinycore.sh"
        
        # 运行Docker构建
        docker run --rm \
          -v "$(pwd)/assets/ezopwrt.img:/mnt/input.img:ro" \
          -v "$(pwd)/build-tinycore.sh:/builder/build.sh:ro" \
          -v "$(pwd)/output:/builder/output" \
          openwrt-builder \
          "bash -c 'chmod +x /builder/build.sh && /builder/build.sh /mnt/input.img /builder/output openwrt-installer.iso'"
    
        # 检查构建结果
        if [ $? -eq 0 ]; then
        echo "✅ ISO构建完成"
        else
        echo "❌ ISO构建失败"
        exit 1
        fi

    - name: 验证ISO文件
      run: |
        if ls output/*.iso 1>/dev/null 2>&1; then
            ISO_FILE=$(ls output/*.iso)
            echo "✅ ISO文件生成成功: $(basename $ISO_FILE)"
            echo "文件大小: $(du -h "$ISO_FILE" | cut -f1)"
            
            # 基本验证
            echo ""
            echo "ISO文件验证:"
            file "$ISO_FILE"
            
            # 检查是否支持BIOS/UEFI
            if command -v xorriso >/dev/null 2>&1; then
                echo ""
                echo "引导信息:"
                xorriso -indev "$ISO_FILE" -toc 2>/dev/null | grep -E "(Bootable|El-Torito|UEFI)" || true
            fi
        else
            echo "❌ 没有生成ISO文件"
            exit 1
        fi

    - name: 上传制品
      uses: actions/upload-artifact@v4
      with:
        name: openwrt-installer-iso
        path: output/*.iso
        retention-days: 7
