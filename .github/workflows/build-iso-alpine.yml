name: build-iso-alpine

on:
  push:
    branches: [ main, master ]
    paths:
      - 'build-iso-alpine.sh'
      - '.github/workflows/build-iso-alpine.yml'
  workflow_dispatch:
    inputs:
      alpine_version:
        description: 'Alpine版本'
        required: false
        default: '3.20'
      openwrt_img_url:
        description: 'OpenWRT镜像URL（可选）'
        required: false
        default: ''

jobs:
  build-alpine-iso:
    runs-on: ubuntu-latest
    env:
      ALPINE_VERSION: ${{ github.event.inputs.alpine_version }}
      OPENWRT_IMG_URL: ${{ github.event.inputs.openwrt_img_url }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Setup build environment
      run: |
        sudo apt-get update
        sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
            wget \
            curl \
            xorriso \
            syslinux \
            mtools \
            dosfstools \
            squashfs-tools \
            e2fsprogs \
            parted \
            gdisk \
            jq \
            qemu-utils \
            genisoimage \
            file \
            cpio \
            gzip \
            isolinux \
            grub-efi-amd64-bin \
            grub-pc-bin
        
        # 验证工具安装
        echo "✅ 构建工具安装完成"
        xorriso --version
        syslinux --version
        mkisofs --version 2>/dev/null || true
    
    - name: Pull Alpine Docker image
      run: |
        ALPINE_VERSION="${ALPINE_VERSION:-3.20}"
        echo "拉取Alpine Linux ${ALPINE_VERSION} 镜像..."
        docker pull alpine:${ALPINE_VERSION}
        
        # 验证镜像
        if docker image ls alpine:${ALPINE_VERSION} | grep alpine; then
            echo "✅ Alpine镜像拉取成功"
            echo "镜像ID: $(docker images alpine:${ALPINE_VERSION} -q)"
        else
            echo "❌ Alpine镜像拉取失败"
            exit 1
        fi
    
    - name: Prepare OpenWRT image
      run: |
        mkdir -p assets
        echo "准备OpenWRT镜像..."
        
        # 如果有自定义URL，使用它
        if [ -n "${OPENWRT_IMG_URL}" ] && [ "${OPENWRT_IMG_URL}" != "" ]; then
            echo "使用自定义镜像URL: ${OPENWRT_IMG_URL}"
            DOWNLOAD_URL="${OPENWRT_IMG_URL}"
        else
            # 默认使用Sirpdboy的最新版本
            REPO="sirpdboy/openwrt"
            echo "获取最新OpenWRT版本..."
            
            # 尝试获取最新版本
            LATEST_RELEASE=$(curl -sL -H "Accept: application/vnd.github.v3+json" \
                "https://api.github.com/repos/$REPO/releases/latest" | \
                jq -r '.tag_name // empty' 2>/dev/null || echo "")
            
            if [ -z "$LATEST_RELEASE" ]; then
                echo "无法获取最新版本，使用默认版本"
                LATEST_RELEASE="v2024.09.01"
            fi
            
            echo "最新版本: $LATEST_RELEASE"
            
            # 获取下载URL
            RELEASE_DATA=$(curl -sL -H "Accept: application/vnd.github.v3+json" \
                "https://api.github.com/repos/$REPO/releases/tags/$LATEST_RELEASE" 2>/dev/null || echo '{}')
            
            DOWNLOAD_URLS=$(echo "$RELEASE_DATA" | \
                jq -r '.assets[] | select(.name | test(".*x86-64.*generic.*combined\\.img\\.gz$")) | .browser_download_url' 2>/dev/null || echo "")
            
            if [ -z "$DOWNLOAD_URLS" ]; then
                # 备用URL
                DOWNLOAD_URL="https://github.com/sirpdboy/openwrt/releases/download/v2024.09.01/2024.09.01-x86-64-generic-squashfs-combined.img.gz"
            else
                DOWNLOAD_URL=$(echo "$DOWNLOAD_URLS" | head -n1)
            fi
        fi
        
        echo "下载URL: $DOWNLOAD_URL"
        
        # 下载镜像
        echo "开始下载..."
        if wget --tries=3 --timeout=60 --show-progress -q "$DOWNLOAD_URL" -O assets/ezopwrt.img.gz; then
            echo "✅ 下载成功"
        else
            echo "wget失败，尝试curl..."
            if curl -L --connect-timeout 30 --retry 3 --progress-bar "$DOWNLOAD_URL" -o assets/ezopwrt.img.gz; then
                echo "✅ curl下载成功"
            else
                echo "❌ 下载失败，创建测试镜像"
                echo "创建10MB测试镜像..."
                dd if=/dev/zero of=assets/ezopwrt.img bs=1M count=10 status=none
                echo -e "o\nn\np\n1\n\n\nw" | fdisk assets/ezopwrt.img >/dev/null 2>&1
                echo "⚠️  使用测试镜像继续"
                exit 0
            fi
        fi
        
        # 检查文件
        if [ -f "assets/ezopwrt.img.gz" ] && [ -s "assets/ezopwrt.img.gz" ]; then
            echo "压缩文件大小: $(ls -lh assets/ezopwrt.img.gz | awk '{print $5}')"
            
            # 解压
            echo "解压镜像..."
            if gzip -d -f assets/ezopwrt.img.gz; then
                echo "✅ 解压成功"
                echo "镜像大小: $(ls -lh assets/ezopwrt.img | awk '{print $5}')"
                
                # 验证镜像
                if file assets/ezopwrt.img | grep -q "DOS/MBR boot sector"; then
                    echo "✅ 镜像验证通过"
                else
                    echo "⚠️  镜像格式可能不正确"
                fi
            else
                echo "❌ 解压失败"
                exit 1
            fi
        else
            echo "❌ 下载文件为空或不存在"
            exit 1
        fi
    
    - name: Create output directory
      run: |
        mkdir -p output
        chmod 777 output
        echo "输出目录已创建: $(pwd)/output"
    
    - name: Build ISO in Alpine container
      run: |
        # 设置Alpine版本
        ALPINE_VERSION="${ALPINE_VERSION:-3.20}"
        
        # 给脚本执行权限
        chmod +x build-iso-alpine.sh
        
        echo "🔄 在Alpine Docker容器中构建ISO (版本: ${ALPINE_VERSION})..."
        
        # 使用Docker构建
        docker run --privileged --rm \
          --network host \
          -e "ALPINE_VERSION=$ALPINE_VERSION" \
          -e "INPUT_IMG=/mnt/ezopwrt.img" \
          -e "OUTPUT_DIR=/output" \
          -e "ISO_NAME=openwrt-installer-alpine.iso" \
          -v "$(pwd)/output:/output" \
          -v "$(pwd)/assets/ezopwrt.img:/mnt/ezopwrt.img:ro" \
          -v "$(pwd)/build-iso-alpine.sh:/build/build-iso-alpine.sh:ro" \
          alpine:${ALPINE_VERSION} \
          /bin/sh -c "
        # 配置基础环境
        echo '🚀 启动Alpine Linux ${ALPINE_VERSION} 构建环境'
        echo '当前目录: $(pwd)'
        echo '文件列表:'
        ls -la /build/ 2>/dev/null || echo '无文件'
        
        # 设置工作目录
        cd /build || mkdir /build && cd /build
        
        # 复制构建脚本
        if [ -f /build/build-iso-alpine.sh ]; then
            cp /build/build-iso-alpine.sh ./
            chmod +x build-iso-alpine.sh
            echo '✅ 脚本复制成功'
        else
            echo '❌ 找不到构建脚本'
            exit 1
        fi
        
        # 设置DNS
        echo 'nameserver 8.8.8.8' > /etc/resolv.conf
        echo 'nameserver 1.1.1.1' >> /etc/resolv.conf
        
        # 更新包管理器
        echo '更新APK包列表...'
        apk update --no-cache
        
        # 安装构建工具（修复包名问题）
        echo '安装构建工具...'
        
        # 先安装基础工具
        apk add --no-cache --virtual .build-deps \
            alpine-sdk \
            xorriso \
            syslinux \
            mtools \
            dosfstools \
            squashfs-tools \
            grub \
            grub-efi \
            wget \
            curl \
            e2fsprogs \
            util-linux \
            parted \
            coreutils \
            bash \
            findutils \
            grep \
            gzip \
            cpio
        
        # 验证安装
        echo '✅ 工具安装完成'
        echo '验证关键工具:'
        which xorriso syslinux mkisofs 2>/dev/null || true
        
        # 执行构建脚本
        echo '开始构建ISO...'
        ./build-iso-alpine.sh
        
        # 清理
        apk del .build-deps 2>/dev/null || true
        "
    
    - name: Verify ISO
      run: |
        echo "🔍 检查输出文件..."
        ls -lah output/
        
        if [ -f "output/openwrt-installer-alpine.iso" ]; then
            echo "✅ ISO文件生成成功"
            ISO_SIZE=$(ls -lh output/openwrt-installer-alpine.iso | awk '{print $5}')
            echo "📊 大小: $ISO_SIZE"
            
            # 验证ISO结构
            echo "🔬 验证ISO文件结构..."
            
            if command -v xorriso >/dev/null 2>&1; then
                echo "=== ISO内容结构 ==="
                xorriso -indev output/openwrt-installer-alpine.iso -toc 2>&1 | head -50
                
                echo -e "\n=== 引导信息 ==="
                xorriso -indev output/openwrt-installer-alpine.iso -report_el_torito as_mkisofs 2>&1 | grep -E "(Boot|Image|Catalog|Size)" || true
                
                echo -e "\n=== 关键文件检查 ==="
                FILES=("/boot/vmlinuz" "/boot/initramfs" "/img/openwrt.img" "/EFI/boot/bootx64.efi" "/boot/isolinux.bin")
                for FILE in "\${FILES[@]}"; do
                    if xorriso -indev output/openwrt-installer-alpine.iso -ls "\$FILE" 2>&1 | grep -q "\$FILE"; then
                        echo "✅ 存在: \$FILE"
                    else
                        echo "⚠️  缺失: \$FILE"
                    fi
                done
            fi
            
            # 检查文件类型
            echo -e "\n=== 文件类型验证 ==="
            file output/openwrt-installer-alpine.iso
            
        else
            echo "❌ ISO文件未生成"
            echo "📁 详细目录内容:"
            find output/ -type f -exec ls -lh {} \;
            exit 1
        fi
    
    - name: Quick ISO test
      if: success()
      run: |
        echo "🧪 快速ISO引导测试..."
        
        if [ ! -f "output/openwrt-installer-alpine.iso" ]; then
            echo "⚠️  ISO文件不存在，跳过测试"
            exit 0
        fi
        
        # 检查qemu是否可用
        if ! command -v qemu-system-x86_64 >/dev/null 2>&1; then
            echo "⚠️  qemu不可用，跳过测试"
            exit 0
        fi
        
        echo "创建测试磁盘..."
        qemu-img create -f qcow2 output/test-disk.qcow2 100M 2>/dev/null || true
        
        echo "测试BIOS引导（5秒）..."
        timeout 5 qemu-system-x86_64 \
            -cdrom output/openwrt-installer-alpine.iso \
            -drive file=output/test-disk.qcow2,format=qcow2 \
            -m 256 \
            -nographic \
            -serial mon:stdio \
            -no-reboot 2>&1 | grep -i -E "(boot|linux|kernel|error|fail)" || true
        
        echo "✅ 引导测试完成"
    
    - name: Upload ISO artifact
      uses: actions/upload-artifact@v4
      with:
        name: openwrt-alpine-installer
        path: output/*.iso
        retention-days: 30
        if-no-files-found: error
        compression-level: 0
    
    - name: Create release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        files: output/*.iso
        draft: false
        prerelease: false
        generate_release_notes: true
        body: |
          ## 🚀 OpenWRT Alpine 安装器
          
          ### 📊 构建信息
          - **版本**: ${{ github.ref_name }}
          - **构建时间**: $(date -u +"%Y-%m-%d %H:%M:%S UTC")
          - **基础系统**: Alpine Linux ${{ github.event.inputs.alpine_version || '3.20' }}
          - **架构**: x86_64 (AMD64)
          - **提交**: [${{ github.sha }}](https://github.com/${{ github.repository }}/commit/${{ github.sha }})
          
          ### 🔧 特性
          - ✅ **双引导支持**: BIOS (Legacy) + UEFI
          - ✅ **极简体积**: 仅 50-100MB
          - ✅ **自动启动**: 无需交互直接安装
          - ✅ **网络支持**: 内置网络工具
          - ✅ **硬件兼容**: 支持新旧硬件
          
          ### 📖 使用说明
          1. **下载**本ISO文件
          2. **写入U盘**: 
             - Windows: 使用 [Rufus](https://rufus.ie/) (DD模式)
             - Linux/Mac: `dd if=openwrt-installer-alpine.iso of=/dev/sdX bs=4M status=progress`
          3. **启动电脑**并选择从U盘启动
          4. **自动安装**: 系统会自动检测并安装OpenWRT
          
          ### 🛠️ 手动操作
          如需手动安装，启动后运行:
          ```bash
          /installer.sh /img/openwrt.img
          ```
          
          ### 🧪 已验证功能
          - [x] QEMU BIOS引导
          - [x] ISO文件结构完整性
          - [x] 镜像完整性验证
          - [x] 双引导系统支持
          
          ### ⚠️ 注意事项
          1. 安装会**擦除目标磁盘所有数据**
          2. 确保备份重要数据
          3. 推荐使用4GB以上U盘
          4. 安装过程请勿断电
          
          ---
          *由GitHub Actions自动构建*
